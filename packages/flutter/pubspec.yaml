name: auto_topup
description: Auto Topup subscription widget for Flutter — airtime and data auto-renewal via Retailcode.
version: 0.1.1
homepage: https://github.com/FBIS-Tech/auto-topup-sdk

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
